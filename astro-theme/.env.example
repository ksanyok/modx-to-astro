# ─── Keystatic CMS Storage ──────────────────────────────────────────────────
#
# Set KEYSTATIC_STORAGE_KIND=gitlab to use GitLab-based Git storage.
# Leave unset (or set to 'local') for local filesystem storage (development).
#
# GitLab storage requires:
#   KEYSTATIC_GITLAB_GROUP   — GitLab namespace / group name (e.g. "my-org")
#   KEYSTATIC_GITLAB_PROJECT — repository name            (e.g. "modx-to-astro")
#
# Example:
KEYSTATIC_STORAGE_KIND=gitlab
KEYSTATIC_GITLAB_GROUP=my-org
KEYSTATIC_GITLAB_PROJECT=modx-to-astro

# ─── Enable Keystatic admin UI in dev ───────────────────────────────────────
# Set to true to activate /keystatic route and server-mode during `npm run dev`
KEYSTATIC=true
